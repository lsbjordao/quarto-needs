# Architecture: where requirements become design

Requirements describe obligations; architecture explains how the product is organized to satisfy them. Quarto-Needs keeps those concerns distinct and links them explicitly.

## System and its context

::: {.need #SYS-QUARTO-NEEDS type="system" status="approved" tags="architecture;c4"}
## Quarto-Needs

The engineering-traceability tool this case study documents: a requirements/decisions/risk/test/evidence graph authored in Quarto Markdown, analyzed by a Python core, and presented through a Quarto extension.
:::

::: {.need #ACTOR-ENGINEER type="actor" status="approved" tags="architecture;c4" depends-on="SYS-QUARTO-NEEDS"}
## Requirements engineer

Authors, reviews, and approves engineering objects; reads generated views to understand traceability and coverage.
:::

::: {.need #EXT-GITHUB type="external-system" status="approved" tags="architecture;c4;github" depends-on="SYS-QUARTO-NEEDS"}
## GitHub

Hosts the project's issues, which Quarto-Needs federates as read-only, provenance-bound external observations (see `interoperability.qmd`).
:::

::: {.need #EXT-OSLC type="external-system" status="approved" tags="architecture;c4;oslc" depends-on="SYS-QUARTO-NEEDS"}
## OSLC-compliant requirements management tool

An external OSLC Requirements Management provider Quarto-Needs federates from as a read-only, provenance-bound source (see `interoperability.qmd`).
:::

## Containers

::: {.need #CONTAINER-PYTHON-PKG type="container" status="approved" tags="architecture;c4" technology="Python 3.12" part-of="SYS-QUARTO-NEEDS"}
## Python package

The installable `quarto-needs` library and CLI: parsing, analysis, governance rules, graph projection, baselines, and localization checks.
:::

::: {.need #CONTAINER-QUARTO-EXT type="container" status="approved" tags="architecture;c4" technology="Quarto/Pandoc Lua filters" part-of="SYS-QUARTO-NEEDS"}
## Quarto presentation extension

The `_extensions/quarto-needs/` bundle: build-time Lua filters and the browser-side JavaScript they ship, together as one installed Quarto extension.
:::

## Components

::: {.need #COMP-PARSER type="component" status="implemented" tags="python;parser;authoring" part-of="CONTAINER-PYTHON-PKG"}
## Declaration parser

Reads QMD declarations and produces structured declaration batches with source locations.
:::

::: {.need #COMP-ANALYSIS type="component" status="implemented" tags="python;semantics;graph" part-of="CONTAINER-PYTHON-PKG"}
## Semantic analysis core

Builds the canonical engineering graph, resolves relation semantics, and produces immutable analysis snapshots.
:::

::: {.need #COMP-RULES type="component" status="implemented" tags="python;governance;validation" part-of="CONTAINER-PYTHON-PKG"}
## Governance rule engine

Evaluates structural, requirement, decision, risk, and evidence governance against the canonical snapshot.
:::

::: {.need #COMP-CLI type="component" status="implemented" tags="python;cli;ci" part-of="CONTAINER-PYTHON-PKG"}
## Command-line interface

Exposes scan, check, coverage, baseline, diff, impact, and related engineering workflows to local and CI execution.
:::

::: {.need #COMP-EXTENSION type="component" status="implemented" tags="quarto;lua;documentation" part-of="CONTAINER-QUARTO-EXT"}
## Quarto presentation extension

Turns the resolved engineering model into cards, tables, matrices, dashboards, inspectors, diagrams, and progressive HTML views.
:::

::: {.need #COMP-GRAPH type="component" status="implemented" tags="graph;cytoscape;projection" part-of="CONTAINER-PYTHON-PKG"}
## Graph projection and exploration subsystem

Publishes bounded public graph projections and enhances them with Cytoscape-based semantic exploration.
:::

::: {.need #COMP-BASELINE type="component" status="implemented" tags="baseline;diff;impact" part-of="CONTAINER-PYTHON-PKG"}
## Baseline and comparison subsystem

Captures reproducible engineering states and computes structured changes and impact paths between states.
:::

::: {.need #COMP-I18N type="component" status="implemented" tags="localization;i18n;babelquarto" part-of="CONTAINER-PYTHON-PKG"}
## Localization projection subsystem

Validates translated sources against the canonical engineering graph and emits presentation-only localized title projections.
:::

## Interfaces

::: {.need #IF-001 type="interface" status="implemented" tags="qmd;authoring;source"}
## QMD engineering declaration interface

The author-facing contract for declaring typed engineering objects and relations in ordinary Quarto Markdown.
:::

::: {.need #IF-002 type="interface" status="implemented" tags="json;snapshot;machine-readable"}
## Canonical engineering snapshot

The machine-readable representation consumed by governance, generated views, comparison workflows, and integrations.
:::

::: {.need #IF-003 type="interface" status="implemented" tags="json;graph;projection"}
## Public graph projection contract

A bounded JSON contract containing approved node/edge fields, relation semantics, traversal profiles, type roles, and safe provenance.
:::

::: {.need #IF-004 type="interface" status="implemented" tags="cli;automation;ci"}
## CLI automation interface

The stable command surface used by developers and automation to invoke engineering analysis.
:::

## Engineering risks

::: {.need #RISK-001 type="risk" status="approved" priority="critical" tags="semantics;drift;architecture"}
## Semantic meaning diverges across core and presentation layers

Duplicated relation semantics can cause Python, Lua, and JavaScript to disagree about the same engineering graph.
:::

::: {.need #RISK-002 type="risk" status="approved" priority="critical" tags="privacy;security;projection"}
## Public documentation leaks internal source information

Graph or diagnostic projections could accidentally expose absolute paths, arbitrary attributes, or internal source content.
:::

::: {.need #RISK-003 type="risk" status="approved" priority="medium" tags="graph;performance;usability"}
## Large graph projections overwhelm interactive documentation

Unbounded projections can become slow, visually unreadable, and unsuitable for review.
:::

::: {.need #RISK-004 type="risk" status="approved" priority="high" tags="localization;semantics;drift"}
## Translations create a competing engineering model

Localized files can silently diverge from canonical IDs, metadata, and relations unless semantic parity is enforced.
:::

::: {.need #RISK-013 type="risk" status="approved" priority="high" tags="distribution;provisioning;python;pip;first-render"}
## First-use provisioning depends on Python, pip, and package availability

A clean machine without a usable interpreter, without pip, or without a reachable package index cannot provision the engine on first render. A silent or vague failure there would strand new users at their very first command.
:::

## Architecture decisions

::: {.need #ADR-001 type="architecture-decision" status="accepted" date="2026-08-29" revisit-after="2027-08-29" decision-makers="Quarto-Needs maintainers" tags="python;semantics;architecture" addresses="SYS-001;SYS-003;NFR-005;RISK-001" applies-to="COMP-ANALYSIS;IF-002" confirmed-by="TC-001;TC-008"}
## Keep Python as the semantic authority

### Context and Problem Statement

The product has a Python analysis core, Lua rendering filters, and JavaScript interaction. If each layer defines engineering meaning independently, equivalent views can drift.

### Decision Drivers

- one source of engineering semantics;
- deterministic analysis;
- testability outside the browser;
- presentation layers that remain replaceable.

### Considered Options

- duplicate relation maps in every client;
- resolve semantics in Lua during rendering;
- resolve semantics once in Python and publish semantic projections.

### Decision Outcome

Python owns engineering semantics. Lua and JavaScript consume relation metadata and other semantic projections emitted by the core.

### Consequences

Public projection contracts become more important, but presentation clients become simpler and cannot silently redefine engineering meaning.

### Confirmation

`TC-001` confirms the canonical snapshot and `TC-008` confirms relation-catalog semantics.
:::

::: {.need #ADR-002 type="architecture-decision" status="accepted" date="2026-08-29" revisit-after="2027-08-29" decision-makers="Quarto-Needs maintainers" tags="quarto;documentation;architecture" addresses="SYS-002;NFR-003" applies-to="COMP-EXTENSION;IF-001" confirmed-by="TC-002"}
## Use Quarto as the executable documentation interface

### Context and Problem Statement

The engineering model needs both formal structure and high-quality human explanation.

### Decision Drivers

- text and Git workflows;
- publishable documentation;
- Markdown narrative;
- extension-based generated views.

### Decision Outcome

Use Quarto documents as the authoring and publishing interface while keeping engineering semantics in the Python core.

### Consequences

The product remains compatible with ordinary documentation workflows and can progressively enhance HTML without making HTML the source of truth.

### Confirmation

`TC-002` renders the book and verifies the static/progressive presentation path.
:::

::: {.need #ADR-003 type="architecture-decision" status="accepted" date="2026-08-29" revisit-after="2027-08-29" decision-makers="Quarto-Needs maintainers" tags="graph;model;traceability" addresses="SYS-001;FUN-002" applies-to="COMP-ANALYSIS;IF-002" confirmed-by="TC-008"}
## Model traceability as a typed property graph

### Context and Problem Statement

Requirements, decisions, risks, implementation, tests, and evidence need a common relationship model without collapsing their distinct meanings.

### Decision Outcome

Represent engineering objects as typed nodes and engineering relations as typed edges with canonical semantic families and directions.

### Consequences

Traceability, impact analysis, queries, and generated views can operate over one model while object roles remain explicit.

### Confirmation

`TC-008` verifies the relation catalog and canonical semantic projection.
:::

::: {.need #ADR-004 type="architecture-decision" status="accepted" date="2026-08-29" revisit-after="2027-08-29" decision-makers="Quarto-Needs maintainers" tags="graph;progressive-enhancement;accessibility" addresses="SYS-006;NFR-003;FUN-004" applies-to="COMP-GRAPH;COMP-EXTENSION;IF-003" confirmed-by="TC-006;TC-010"}
## Drive static and interactive graphs from the same projection

### Context and Problem Statement

Separate graph-generation paths can cause the accessible static representation and the interactive Cytoscape view to disagree.

### Decision Outcome

Generate one bounded public projection and derive static diagram/table output and interactive exploration from that same artifact.

### Consequences

The public graph schema becomes a stable contract and interactive features must operate as progressive enhancement over it.

### Confirmation

`TC-006` validates the public projection contract and `TC-010` checks the exploration assets.
:::

::: {.need #ADR-005 type="architecture-decision" status="accepted" date="2026-08-29" revisit-after="2027-08-29" decision-makers="Quarto-Needs maintainers" tags="localization;i18n;semantics" addresses="SYS-005;FUN-006;RISK-004" applies-to="COMP-I18N" confirmed-by="TC-005"}
## Keep English canonical and localization presentation-only

### Context and Problem Statement

A multilingual book should not create two engineering graphs that can evolve independently.

### Decision Outcome

English QMD files are canonical engineering sources; `*.pt-BR.qmd` siblings may translate presentation text only. Pre-render validation rejects semantic differences.

### Consequences

Translations remain useful to readers while governance, baselines, impact analysis, and machine-readable exports stay language-neutral.

### Confirmation

`TC-005` checks semantic parity and localized title projection generation.
:::

::: {.need #ADR-006 type="architecture-decision" status="accepted" date="2026-08-29" revisit-after="2027-08-29" decision-makers="Quarto-Needs maintainers" tags="query;graph;views" addresses="FUN-003" applies-to="COMP-ANALYSIS;COMP-GRAPH" confirmed-by="TC-009"}
## Evaluate named queries in the core and materialize reusable views

### Context and Problem Statement

Reimplementing query semantics in Lua would create another semantic authority and make graph views harder to reproduce.

### Decision Outcome

Named queries are evaluated by the Python core. Their result sets can be materialized as bounded graph views and consumed by shortcodes through a view manifest.

### Consequences

Selection remains deterministic and reusable across tables and graphs; presentation layers only request an already resolved view.

### Confirmation

`TC-009` verifies named-query graph materialization and manifest resolution.
:::

::: {.need #ADR-007 type="architecture-decision" status="accepted" date="2026-08-30" revisit-after="2027-08-30" decision-makers="Quarto-Needs maintainers" tags="quarto;toc;sidebar;usability;progressive-enhancement" addresses="FUN-009" applies-to="COMP-EXTENSION" confirmed-by="TC-014"}
## Treat the margin TOC control as persistent progressive enhancement

### Context and Problem Statement

The page margin TOC can consume useful reading width, but a collapse control positioned over the TOC undermines the navigation it is intended to improve.

### Decision Drivers

- preserve native Quarto navigation semantics;
- make the feature optional and browser-only;
- persist the reader's collapsed state;
- reclaim content width while collapsed;
- never obscure the expanded TOC with the control itself.

### Decision Outcome

Implement the control as JavaScript progressive enhancement. Persist state in local storage, use Quarto's `fullcontent` class only while the extension forces the collapsed state, and position the expanded control completely outside the margin sidebar with an explicit gap.

### Consequences

The static document remains usable without JavaScript, while HTML readers gain a persistent space-reclaiming control. Positioning becomes an explicit regression contract rather than incidental CSS/DOM behavior.

### Confirmation

`TC-014` checks persistence/full-content behavior and verifies that the expanded control is positioned by `button.offsetWidth + gap`, never by the old half-width overlap.
:::

::: {.need #ADR-011 type="architecture-decision" status="accepted" date="2026-09-02" revisit-after="2027-09-02" decision-makers="Quarto-Needs maintainers" tags="documentation;architecture;viewpoints;arc42" addresses="STK-002;SYS-002;RISK-001" applies-to="COMP-EXTENSION" confirmed-by="TC-002"}
## Document Quarto-Needs itself through explicit viewpoints, indexed arc42-lite

### Context and Problem Statement

`ARCHITECTURE.md` and this self-hosted example both describe the system, but neither page declares its audience, the concern it answers, or when it must be revisited. As the product grew from a single pipeline into CLI/core/Quarto/LSP/VS Code/interoperability surfaces, an undeclared set of ad hoc pages risks duplicating content and drifting silently.

### Decision Drivers

- give every architecture page an explicit audience, concern, and update trigger;
- avoid forcing one notation (C4, UML, or 4+1) to answer every question;
- keep the index navigable without becoming a second copy of this case study.

### Decision Outcome

Organize `notes/architecture/` as a small set of ISO/IEC/IEEE 42010-style viewpoints, indexed by an arc42-lite structure. Each page records its audience, concern, notation, and source in `viewpoints.md`; diagrams are chosen per concern rather than committing the whole set to one modeling language.

### Consequences

The documentation set gains a stable table of contents and a contract per page, at the cost of keeping `viewpoints.md` current whenever a page's purpose changes.

### Confirmation

`TC-002` confirms the presentation/rendering path this documentation set builds on.
:::

::: {.need #ADR-012 type="architecture-decision" status="accepted" date="2026-09-02" revisit-after="2027-09-02" decision-makers="Quarto-Needs maintainers" tags="architecture;types;extensibility" addresses="SYS-004;RISK-001" applies-to="COMP-ANALYSIS;COMP-RULES" confirmed-by="TC-004"}
## Model architectural roles as configurable types, not a fixed enumeration

### Context and Problem Statement

Phase 6 needed `system`/`container`/`component`/`actor`/`external-system` roles for C4 views. A fixed Python enum for architectural roles would hardcode one project's vocabulary into the core, contradicting the existing decision to keep traceability as a typed property graph with project-declared types (`ADR-003`).

### Decision Outcome

Architectural roles are ordinary entries under `[types.*]` in project configuration, validated the same way as every other type, never a hardcoded enumeration in `config.py`. `ARC001` and the C4 projection match on these configured types plus the fixed C4 layer order, never on a Python-level role constant.

### Consequences

A project can rename or extend its architectural vocabulary without a core code change; C4 support depends on a project actually declaring the expected types, which the C4 projection and `ARC001` already report explicitly when a focus type is unsupported.

### Confirmation

`TC-004` confirms decision and rule governance, including `ARC001`'s layer-adjacency checks.
:::

::: {.need #ADR-013 type="architecture-decision" status="accepted" date="2026-09-02" revisit-after="2027-09-02" decision-makers="Quarto-Needs maintainers" tags="architecture;hierarchy;relations" addresses="SYS-004;RISK-001" applies-to="COMP-ANALYSIS;COMP-RULES" confirmed-by="TC-004"}
## Represent architecture hierarchy and containment through a first-class part-of relation

### Context and Problem Statement

`decomposes` already expressed parent-to-child containment, but nothing let a child self-declare its parent: every container had to enumerate its own components by hand, an error-prone, one-directional bookkeeping burden.

### Decision Outcome

Add `part-of` as `decomposes`'s genuine inverse (child-authored, `(part, whole)` roles, its own `v1_name` so `relation_policies` can target one direction independently). Enforce fixed-layer adjacency (`system > container > component > source-module`) with the `ARC001` rule over both authored directions.

### Consequences

Components and source modules author their own placement; `ARC001` catches layer-skipping or same-layer edges as a structural error, and non-C4 types are simply ignored by the rule rather than rejected.

### Confirmation

`TC-004`.
:::

::: {.need #ADR-014 type="architecture-decision" status="accepted" date="2026-09-02" revisit-after="2027-09-02" decision-makers="Quarto-Needs maintainers" tags="architecture;relations;traceability" addresses="RISK-001" applies-to="COMP-ANALYSIS;COMP-RULES" confirmed-by="TC-004"}
## Reuse part-of instead of introducing a separate realized-by/realizes relation

### Context and Problem Statement

An external documentation-strategy review proposed adding an `ArchitectureElement realized-by ImplementationArtifact` relation (and its inverse `realizes`) to connect components to the source modules that implement them, reasoning that containment and "is realized by" are conceptually distinct. By the time this decision was recorded, `part-of` already connected every `source-module` to its owning `component` (see `implementation.qmd`), and `ARC001` already treats that edge as the bottom rung of the same fixed C4 layer ladder used for `container`/`system`.

### Considered Options

- introduce `realized-by`/`realizes` as a new relation family, kept parallel to `part-of`;
- keep `part-of` as the single relation connecting a source module to its owning component, exactly as it already connects a component to its container and a container to its system.

### Decision Outcome

Do not introduce `realized-by`/`realizes`. `part-of` already means "the child sits inside this parent at the next C4 layer up" for every layer, including `source-module -> component`. A second relation expressing the same fact between the same two objects would let the two edges disagree with no rule capable of detecting the contradiction, and would fragment `ARC001`'s single layer-adjacency check across two relation families.

### Consequences

Architecture-to-implementation traceability continues to read entirely through `part-of` plus `implements`/`implemented-by` (code to requirement); no new relation, rule, or projection code exists for this concern.

### Confirmation

`TC-004`.
:::

::: {.need #ADR-015 type="architecture-decision" status="accepted" date="2026-09-02" revisit-after="2027-09-02" decision-makers="Quarto-Needs maintainers" tags="architecture;c4;projection" addresses="SYS-006;RISK-001;RISK-002" applies-to="COMP-GRAPH" confirmed-by="TC-006"}
## Derive C4/architecture projections from the canonical snapshot, never hand-author them

### Context and Problem Statement

A hand-maintained C4 diagram drifts the moment the underlying `part-of`/`depends-on` graph changes, and a second, independently authored diagram risks disagreeing with the engineering graph it claims to depict.

### Decision Outcome

`build_c4_view` computes every C4 level as one `select_graph` depth-1 call over an allowlisted set of relations for that level; `c4_mermaid_source`/`c4_code_table_markdown` render that projection as text; `write_c4_projections` writes one JSON file per view under `.quarto-needs/graphs/`. No C4 view is authored directly; the `need-c4` shortcode only ever reads a pre-rendered projection file.

### Consequences

Every C4 diagram in this documentation set is reproducible from the graph and cannot silently diverge from it; adding an object or `part-of` edge is sufficient to update the corresponding view on the next build.

### Confirmation

`TC-006`.
:::

::: {.need #ADR-016 type="architecture-decision" status="accepted" date="2026-09-02" revisit-after="2027-09-02" decision-makers="Quarto-Needs maintainers" tags="architecture;renderer-independence" addresses="SYS-002;RISK-001" applies-to="COMP-EXTENSION;COMP-GRAPH" confirmed-by="TC-002"}
## Keep the architecture model renderer-independent

### Context and Problem Statement

C4/UML-flavored diagram text (Mermaid) is one presentation of the architecture graph; a second, unrelated presentation — the interactive Cytoscape exploration view — already exists over the same public projection.

### Decision Outcome

Mermaid source, the Code-level Markdown table, and the interactive Cytoscape view are alternate renderers over the same `graph_projection.py` output; none of them is treated as the canonical architecture model, and none of the three defines relation semantics of its own.

### Consequences

A future renderer (a different diagram language, a static image export) can consume the same projection without touching `analysis.py`, `relations.py`, or the C4 projection code.

### Confirmation

`TC-002`.
:::

::: {.need #ADR-017 type="architecture-decision" status="accepted" date="2026-09-02" revisit-after="2027-09-02" decision-makers="Quarto-Needs maintainers" tags="architecture;generated-artifacts;boundary" addresses="RISK-002;SYS-006" applies-to="COMP-GRAPH" confirmed-by="TC-006"}
## Draw an explicit boundary between authored architecture declarations and generated projections

### Context and Problem Statement

Generated files (`.quarto-needs/needs.json`, `.quarto-needs/graphs/*.json`, `generated-index.lua`) live inside the same project tree as authored `.qmd` declarations. Without an explicit rule, a contributor could hand-edit a generated file, or a generated file could be mistaken for a second authoring surface.

### Decision Outcome

Every path under a project's `.quarto-needs/` output directory is generated-only: the CLI build step recreates it in full on every run, `generated-index.lua` is explicitly excluded from the extension-asset sync so it is never treated as an authoring source, and stale `c4-*.json` files are removed before a projection is rewritten. `notes/architecture/generated/` follows the same rule for this documentation set's own diagrams.

### Consequences

Reviewers and contributors can rely on "is it under `.quarto-needs/` (or `notes/architecture/generated/`)?" as a sufficient test for "may I hand-edit this file?"; the answer is always no.

### Confirmation

`TC-006`.
:::

::: {.need #ADR-018 type="architecture-decision" status="accepted" date="2026-09-02" revisit-after="2027-09-02" decision-makers="Quarto-Needs maintainers" tags="architecture;c4;execution-units" addresses="SYS-004;RISK-001" applies-to="COMP-ANALYSIS;COMP-RULES" confirmed-by="TC-004"}
## Classify execution units (system, container) separately from logical components

### Context and Problem Statement

This example's own `component` objects mix logical responsibility (parser, rule engine) with what are really deployable/executable units; a naive C4 projection that mapped every `type=component` to a C4 Component would blur the distinction between "runs as a process" and "is a logical piece of a process."

### Decision Outcome

`system` and `container` are configured as distinct types representing deployable/executable units; `component` remains the logical decomposition inside a container. `build_c4_view` requires a `system` focus for Context/Container and a `container` focus for Component, so a logical component can never itself be treated as a container boundary.

### Consequences

Adding a new deployable unit means declaring a new `container` (or `system`), not repurposing an existing `component`; `ARC001`'s fixed layer order (`system > container > component > source-module`) enforces this separation structurally.

### Confirmation

`TC-004`.
:::

::: {.need #ADR-019 type="architecture-decision" status="accepted" date="2026-09-03" revisit-after="2027-09-03" decision-makers="Quarto-Needs maintainers" tags="distribution;extension;bootstrap;managed-runtime" addresses="SYS-009;RISK-013" applies-to="COMP-EXTENSION" confirmed-by="TC-025;TC-026;TC-029"}
## Provision a project-local managed runtime through an extension bootstrap

### Context and Problem Statement

The extension needs a Python semantic engine, but users install only the extension. The old contract asked for three separate pieces — a global pip install, a CLI on PATH, and a hand-authored pre-render — each one an independent chance to be missing, stale, or wrong for the extension's version.

### Decision Outcome

The extension manifest contributes the project pre-render itself. A standard-library bootstrap provisions `quarto-needs==<extension version>` into `.quarto-needs/runtime/<version>/<interpreter-identity>/` through staged, lock-protected, atomic installs, validates the actually imported version, and invokes the same canonical pre-render service the CLI uses. A valid cached runtime renders offline; a provisioning failure fails the render with the needed version, the missing runtime, and both recovery actions.

### Consequences

Rendering has exactly one installation responsibility, and extension and engine versions cannot drift silently. First-use provisioning still requires Python and pip, which remain documented platform prerequisites; the residual availability risk is carried explicitly by `RISK-013` rather than hidden.

### Confirmation

`TC-025` proves the clean first render, `TC-026` the offline second render, and `TC-029` concurrent-safe provisioning.
:::

## Generated architecture views

The book records {{< need-count types="architecture-decision" >}} architecture decisions, {{< adr-count status="accepted" >}} of them accepted. Both numbers are read from the graph, so the first proposed or superseded decision changes them without anyone editing this sentence.

`adr-table` is the decision-aware view: it selects only `architecture-decision` objects and understands their date fields, so decisions sort newest-first and can be bounded by a review window.

{{< adr-table status="accepted" columns="id;title;date;decision-makers;tags" date-sort="desc" caption="Accepted decisions, most recent first" id="accepted-decisions" >}}

{{< need-graph view="architecture-decisions" id="decision-view" >}}

## Generated C4 views

{{< need-c4 root="SYS-QUARTO-NEEDS" level="context" >}}

{{< need-c4 root="SYS-QUARTO-NEEDS" level="container" >}}

{{< need-c4 root="CONTAINER-PYTHON-PKG" level="component" >}}

## Additional C4 diagram backends

Each `need-c4` view also supports three additional backends, selected with the `backend` shortcode argument. PlantUML, D2, and current Structurizr tooling render diagrams when their local CLIs are available; otherwise the DSL source is shown as a code block:

### Structurizr DSL

{{< need-c4 root="SYS-QUARTO-NEEDS" level="context" backend="structurizr" >}}

### C4-PlantUML

{{< need-c4 root="SYS-QUARTO-NEEDS" level="context" backend="plantuml" >}}

### D2

{{< need-c4 root="SYS-QUARTO-NEEDS" level="context" backend="d2" >}}

::: {.need #ADR-020 type="architecture-decision" status="accepted" date="2026-09-09" revisit-after="2027-09-09" decision-makers="Quarto-Needs maintainers" tags="distribution;release;support" addresses="SYS-009;RISK-013" applies-to="COMP-EXTENSION" confirmed-by="TC-025;TC-032"}
## Publish a narrow first supported release

### Context and Problem Statement

A single maintainer cannot promise the same release stability for the core engineering workflow and every external integration. The existing v0.1.0 tag reached TestPyPI but failed its installation rehearsal; the current candidate is 0.1.1.

### Decision Outcome

Release 0.1.1 supports the documented core analysis, change reports, evidence, LSP, exports and Quarto views. OSLC, GitHub Issues, migration adapters, the VS Code client and C4 projections remain experimental. Preserve v0.1.0; publish a new immutable version only after clean distribution checks and a TestPyPI rehearsal. Detailed stability tiers are a subsequent policy change.

### Consequences

Release validation covers the supported surface; experimental functionality does not expand that promise. Package schemas and canonical extension assets accompany both distributions, while the extension remains independently installable through Quarto.

### Confirmation

`TC-025`, `TC-032`.
:::
