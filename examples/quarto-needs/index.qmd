# Engineering Quarto-Needs with Quarto-Needs

This book is a **self-hosted engineering case study**: Quarto-Needs is used to specify, explain, govern, verify, and explore the engineering of Quarto-Needs itself.

The example is intentionally different from the Aegis IAM showcase. Aegis demonstrates that the tool works in an external application domain. This case study demonstrates how requirements engineering concepts connect inside the product that implements them.

The canonical engineering graph is authored in English. Every chapter has a Brazilian Portuguese sibling (`*.pt-BR.qmd`) that translates titles and narrative while preserving the same IDs, types, statuses, attributes, and relations. The pre-render step rejects semantic drift between languages.

## What to look for

The book is read from stakeholder concern toward realization and evidence:

```text
Stakeholder need
      ↓ motivates
System requirement
      ↓ is refined into
Functional / non-functional requirement
      ↓ is addressed by
Architecture decision
      ↓ scopes
Component or interface
      ↓ is realized in
Source module
      ↓ is verified through
Test case
      ↓ produces / supports
Evidence
```

That reading order is deliberately **not** the same thing as the direction of every stored relation. For example, the canonical derivation relation is authored as:

```text
Functional requirement --derives-from--> System requirement
System requirement     --derives-from--> Stakeholder need
```

Quarto-Needs can still traverse the derivation chain pedagogically from the stakeholder need toward the derived requirements because traversal direction is semantic metadata published by the core rather than guessed from raw edge orientation.

This is not a synthetic taxonomy. The objects describe capabilities and architectural choices that exist in the repository today.

## The learning path

1. **System Context** establishes the system boundary, actors, and external tools.
2. **Drivers** introduces stakeholder needs and system requirements.
3. **Requirements** refines those drivers into functional and quality requirements.
4. **Architecture** records the components, interfaces, risks, and ADRs that shape the implementation.
5. **Implementation** traces approved obligations into real Python and JavaScript source modules.
6. **Verification** connects requirements and decisions to executable checks and evidence.
7. **Traceability** uses the interactive graph as an analysis instrument rather than a static diagram.
8. **Change** teaches baseline, structured diff, and semantic impact analysis.

## Live overview

The table below is generated from the same engineering graph used by the rest of the book.

{{< need-table types="system-requirement" columns="id;title;status;priority" sort="priority;id" >}}

The graph is progressively enhanced: the static representation remains available, while the HTML view adds semantic traversal, filtering, root paths, edge inspection, collapse/expand, and fixed spacing.

{{< need-graph id="quarto-needs-overview" projection="need-graph-1" >}}
