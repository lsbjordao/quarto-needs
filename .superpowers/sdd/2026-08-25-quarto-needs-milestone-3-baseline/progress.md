# SDD ledger — plan: docs/superpowers/plans/2026-08-25-quarto-needs-milestone-3-baseline.md

Spec: docs/superpowers/specs/2026-08-25-quarto-needs-evolution-design.md (Milestone 3 + "Milestone 3 design decisions")
Execution mode: superpowers:subagent-driven-development, requested explicitly by the user.
Baseline: `.venv/bin/python -m pytest -q` — 175 passed, 2 permitted deprecation warnings on 2026-08-25.

## Preflight rulings

- Ruling: this workspace has no Git metadata, so the skill's `sdd-workspace`, `task-brief`, and `review-package` scripts (all of which shell out to git) are replaced by `task-brief.py`, `snap.sh`, and `review-package.py` in this directory; review ranges are filesystem snapshot pairs compared with `diff -ruN -U10` — if wrong, reviews lose commit-level granularity but keep full file-level diffs and test evidence, exactly as Milestones 1 and 2 ran.
- Ruling: Tasks 6 and 7 are dispatched as ONE implementer unit. Task 6 defines `DiffReport` and the classification helpers but its own Step 7 expects the tests to still FAIL, because `compare` only arrives in Task 7 — so Task 6 carries no green test cycle and is not independently reviewable. Merging them restores the skill's task right-sizing rule (a task is the smallest unit that carries its own test cycle) — if wrong, one review gate covers a larger diff than the plan's authoring implied.
- Ruling: the plan's per-task "conditional checkpoint" steps will print the no-Git message rather than committing; implementers must not initialize a repository — if wrong, recovery depends on the snapshots in this workspace instead of git history.

## Preflight task/interface scan

| Task(s) | Producer -> consumer or self-check | Finding |
|---|---|---|
| 1 | `envelope_validator()` on `referencing` -> Tasks 4/8/10 schema tests | Consistent; Tasks 4, 8, and 10 each build their own `Draft202012Validator` with no `$ref`, so they need no registry. |
| 1 | Own text: `-W error::DeprecationWarning` at Step 4/9 | Consistent; the only warnings in the current suite are the 2 `RefResolver` ones this task removes. |
| 1 | Own text: probes reuse `write_duplicate_project` | Consistent; that helper already exists in `tests/test_cli.py`. |
| 1, 5, 8, 10 | All append to `tests/test_cli.py` | Consistent; each appends distinct test functions and none edits another's. |
| 2 | `canonical_document()` + `RULE_SET_VERSION` -> Task 3 configuration fingerprint | Consistent; Task 3 imports both at module level. |
| 2 | Own text: `config.py` importing `freeze_json`/`thaw_json` from `snapshot.py` | Consistent; `snapshot.py` does not import `config`, so no cycle. Chain is `fingerprints -> rules -> config -> snapshot`. |
| 3 | Fingerprint helpers -> Tasks 4, 6, 7, 9 | Consistent; every caller uses the six exact names the Interfaces block declares. |
| 3 | Own text: defaulted snapshot fields after non-defaulted ones | Consistent; `AnalysisSnapshot` is `frozen=True, slots=True` and the four new fields all carry defaults, so existing direct constructions in tests keep working. |
| 3, 11 | Fingerprints exclude derived data -> Aegis baseline diffs clean | Consistent; Task 11 Step 4 names a non-empty diff at creation as a Task 3 defect, not a regeneration prompt. |
| 4 | Baseline payload key names -> Tasks 6/7 classification | Consistent; `authoredName`, `authoredFingerprint`, `semanticFingerprint`, and `location` are read with exactly those spellings. |
| 4, 5 | `build_baseline(..., queries=...)` -> CLI | Consistent; `materialize_queries` is already imported in `cli.py` from Milestone 2. |
| 4 | Own text: imports `export._write_atomic_text` | Consistent but private; same package, and it is the only atomic writer in the codebase. |
| 5, 8, 10 | Three new subcommands in `cli.py` | Consistent; each registers its own parser and dispatches before the shared `result = analyze_project(...)` line, preserving one analysis per invocation. |
| 6, 7 | `_classify_relations(..., key=)` -> `compare` | Consistent after the merge ruling above; `key` defaults to `semanticFingerprint` and `compare` passes `authoredFingerprint` when the configuration changed. |
| 7 | Own text: guard suppression vs. spec | Consistent; authored object and relation comparison still runs on configuration mismatch, matching the spec's "authored object content and authored relation tuples only". |
| 7, 9 | `DiffReport` fields -> `impact._origins` | Consistent; `_origins` reads `added_objects`, `removed_objects`, `modified`, `added_relations`, `removed_relations`, all declared on the dataclass. |
| 9 | Own text: BFS distance vs. path length | Consistent; breadth-first order makes the first path found the shortest, so `len(path) - 1 == distance` holds, which Task 9's test asserts. |
| 9, 10 | `ImpactReport.to_dict()` -> `impact-v1.schema.json` | Consistent; the schema's `additionalProperties: false` lists exactly the eight keys the traversal emits. |
| 10 | Own text: `impact` calls `compare(recompute=True)` unconditionally | Consistent; the configuration guard above it already rejected real mismatches, and impact needs true classification rather than suppressed deltas. |
| 11 | Round-trip test across a day boundary | Consistent; a changed reference date emits a notice but suppresses derived deltas, so the diff stays empty and the command still exits 0. |

## Execution

- Ruling (preflight, session 2): three plan-mandated test defects found and fixed in plan+brief before dispatch, each verified empirically. (1) Task 4's fixture authored rationale as a `### Rationale` heading, but the parser extracts `rationale` only from metadata lines, so the brief's own assertion `requirement["rationale"].startswith("Protect")` could never pass; fixed by authoring `rationale: Protect data.` as metadata. The Task 4 implementer hit this before the ruling was recorded (first dispatch), stopped clean per the stop-and-report rule, and redid the task against the corrected brief. (2) Tasks 6/7 expected REQ002 to fire for a `functional-requirement`, but REQ002 fires only for `system-requirement`/`software-requirement` (validation.py); the shared REQUIREMENT fixture and Task 7's two inline rewrites now use `system-requirement` — verified that dropping the rationale heading then makes REQ002 appear and nothing else move. (3) Task 9's removal test asserted `REQ-1 in impacted` after removing TC-1, but `verified-by` propagates source_to_target, so the removed test can never reach the requirement it verified, and REQ-1 is itself a `relation-removed` origin (excluded from impacted); per the spec ("removed nodes and edges remain explainable") the test now asserts the two origins, and a new test makes the union falsifiable: dropping only the `conflicts-with` edge (direction `both`) must still impact DOC-1, reachable exclusively through the baseline side. — If any ruling is wrong, the affected task's own TDD run fails and surfaces it before review.
- Ruling (preflight, session 2, continued): (4) Task 11's showcase test removed the edge with `.replace("verified-by: IAM-TC-001", "", 1)`, but `examples/book/requirements/system.qmd` authors relations in Pandoc attribute form (`verified-by="IAM-TC-001"`), so the replace matched nothing and the test would pass vacuously; fixed to the attribute form and verified empirically on a copy of the book — the project stays structurally valid, the IAM-SYS-001→IAM-TC-001 edge disappears, and IAM-FUN-001's parallel edge remains. Same ruling protocol: plan + brief edited together.
- Ruling: the plan's Task 1 snippet passed `default_specification=Draft202012Validator.META_SCHEMA`, which raises `AttributeError` because `META_SCHEMA` is a plain dict, not a `referencing.Specification`. The implementer substituted `referencing.jsonschema.DRAFT202012` after reproducing the failure; I accepted the substitution and corrected the plan text to match — if wrong, a future `referencing` release renames that constant and the snippet needs another adjustment.
- Task 1: implementer DONE (181 passed, 0 warnings; `Checkpoint 1 verified; workspace has no Git metadata.`). First task-review dispatch died early on an account session limit, not on a code defect; re-dispatched after the limit reset. Controller re-verified the tree independently in the meantime: fast group `156 passed in 2.34s`.
- Task 1: review found 1 Important finding, labeled plan-mandated — the cycle probe I wrote into Task 1 Step 7 is a characterization test that passes for the wrong reason. Ruling: the finding is correct and the fix proceeds. In a 2-node cycle where one node is `start`, `_reachable`'s separate `candidate != start` filter already excludes the only back-edge, so deleting the `seen` set would not hang the test; and with a single reachable ID the case-insensitive sort key is never exercised. The regression the test names is therefore undetectable by it — if wrong, we spend one fix round hardening a test that was already adequate, which is the cheap direction of the error.
- Task 1: review confirmed the load-bearing risk I asked it to scrutinize is absent — it proved the `referencing` registry genuinely resolves the envelope's `$ref` by validating the 85-object golden fixture against an empty registry and observing `Unresolvable: needs.schema.json`, rather than accepting a passing test as evidence.
- Task 1: fix round 1/5 (1 addressed, 0 open pending re-review). New fixture is a 3-node graph `REQ-A -> sub-b -> SUB-C -> sub-b`; the subcycle excludes the start node, and the two downstream IDs are cased so plain `sorted()` ("SUB-C" first) disagrees with the casefold order the CLI must produce ("sub-b" first). Falsification evidence: with the `seen` membership check stripped, the test hung and `timeout 10` killed it (exit 143); `cli.py` was restored and verified byte-identical by `diff`. Plan text synced to the built test, with a note recording why the original snippet was wrong.
- Task 1: minor (deferred): the fix report narrates `timeout`'s expiry exit code as 143; GNU coreutils reports 124 on expiry unless `--preserve-status` is passed. The re-reviewer confirmed this does not weaken the hang evidence (a 10s kill on a test that otherwise runs in ~0.1s), but the narrative detail is imprecise.
- Task 1: complete (no commits: no Git metadata; fix round 1/5 verdict "All findings addressed, no new Critical/Important breakage"; re-reviewer independently hand-traced both the subcycle regression and the casefold-vs-ASCII divergence rather than trusting the report).
- Task 2: review found 3 Important findings, all labeled plan-mandated (my plan text specified the defective code verbatim). Rulings below; all three fixes proceed.
- Ruling: finding 1 (a TOML-native date inside a query crashes `canonical_document()` with an unhandled `TypeError` from `freeze_json`) is real and load-bearing. `tomllib` parses a bare `2026-01-01` into `datetime.date`; `load_config` stores query sources without per-value validation; and Task 3 computes the fingerprint inside `analyze_project`, which runs *before* `materialize_queries` ever validates those values. So the crash is reachable on every command and violates the Global Constraint that invalid configuration exits 2. Fix: wrap the queries branch and re-raise as `ConfigurationError` via `_fail`, plus a regression test — if wrong, we convert a crash into a controlled error slightly earlier than a deeper validation pass would have.
- Ruling: finding 2 (`test_canonical_document_is_order_independent` cannot fail) is correct. Python dict equality ignores insertion order, so the assertion holds even with every `sorted()` deleted — the test verifies nothing about the property it names. Fix: assert on the serialized `json.dumps(...)` form, where order is observable — if wrong, the test becomes marginally stricter than the fingerprint strictly requires, which is the safe direction.
- Ruling: finding 3 (the "every policy section" test never exercises `types`) is correct and is precisely the load-bearing scenario I asked the reviewer to hunt: combined with finding 2, no test would catch `types` being dropped from the canonical document entirely, which would make required-attribute policy invisible to the fingerprint guard. Fix: assert the `types` section.
- Ruling: the reviewer's Minor (the docstring claims to exclude a file path that `NeedsConfig` never stores) is accepted and folded into the same fix round, since the file is already open.
- Task 2: fix round 1/5 (3 addressed, 1 open — the open one is the docstring Minor only). Re-reviewer traced the new `except TypeError` branch to confirm the regression test actually reaches it rather than passing on an earlier rejection, and judged both falsification experiments genuine.
- Task 2: minor (deferred): `src/quarto_needs/config.py:107-108` still says `canonical_document` "excludes the configuration file's path", but `NeedsConfig` holds no path field — the method cannot exclude what is never stored. Documentation-only; the corrected wording already exists in the plan text. Minor findings never extend the fix loop, so this is carried to the final whole-branch review rather than re-dispatched.
- Task 2: complete (no commits: no Git metadata; 3 Important findings addressed and verified by falsification; 1 Minor deferred; controller sanity check of the tree `160 passed in 2.27s`).
- Task 3: implementer DONE (192 passed, 0 warnings; `Checkpoint 3 verified; workspace has no Git metadata.`). Step 10 byte-identity gate held: `examples/book/.quarto-needs/needs.json` hashed `d53be57fe6c2e438b7e06083ab64868e6dd3c93e50fedbf45f58a4470a5616da` both before and after, confirming no fingerprint reached the v1 projection. Controller sanity check of the tree: `167 passed in 1.93s`. Implementer noted one wording mismatch with the brief: the missing-module failure surfaced as `ImportError: cannot import name 'fingerprints' from 'quarto_needs'` rather than the brief's stated `ModuleNotFoundError` — same root cause, correct-reason failure.
- Task 3: review returned Task quality Approved with 1 Important finding (plan-mandated) and 2 Minors. Ruling: the Important finding is real and the fix proceeds despite the Approved verdict, because the skill's fix loop triggers on any Important finding. `object_content_fingerprint` hashes `id`, but the mutation table in `test_object_fingerprint_changes_with_every_authored_field` holds `id` constant, so dropping `id` from the digest would go undetected. That matters at graph level: `semantic_graph_fingerprint` hashes the *set* of object content fingerprints, so without `id` a renamed object with unchanged content would leave the graph fingerprint identical and a real change would be invisible — if wrong, we spend one round widening a mutation table, which is the cheap direction.
- Ruling: the two Minors (tags never varied independently; relation `source` never varied independently) are folded into the same fix round rather than deferred, because they are one-line additions to the very table being edited — if wrong, the round is marginally larger than the rules require.
- Task 3: review confirmed the load-bearing alias-flip property holds *in general*, including the same-role case I asked about: for `conflicts-with` (`subject`/`subject`) the role sort ties and degrades to ordering by id, so both authoring directions still canonicalize to the same pair. Derived-value leakage checked clean structurally — `ObjectRecord`/`RelationRecord` have no `href` field at all — and `catalog_name`/`v1_name`/`impact_direction` are correctly kept out of both relation fingerprints.
- Task 3: fix round 1/5 (1 Important + 2 Minors addressed, 0 open). Session was interrupted between applying the fix edits (2026-08-25 21:29) and the fix bookkeeping; on resume the live test already carried all three additions but diverged cosmetically from the plan text synced at 21:28 (`"safety"` vs `"authentication"` for the tags-only row; `source` assertion ordered before `target`). Ruling: align the test to the plan, since later tasks and the whole-branch review read the plan as canonical — if wrong, the diff between the two spellings is semantic noise either way. Falsification run twice independently (controller and re-reviewer): with `"id"` stripped from the `object_content_fingerprint` digest, `test_object_fingerprint_changes_with_every_authored_field` fails exactly on the `("id", "REQ-2")` row; restoration verified byte-identical both times.
- Task 3: complete (no commits: no Git metadata; fix round 1/5 verdict "All findings addressed, no new Critical/Important breakage"; re-reviewer hand-traced all three additions and re-ran the falsification rather than trusting the report; fast groups `19 passed` post-restore; plan text confirmed in sync with the built test).
- Task 4: implementer's first dispatch blocked clean on the plan-mandated fixture defect (rationale heading vs metadata extraction), reverted byte-exact, and redone against the corrected brief — DONE (199 passed, 0 warnings; `Checkpoint 4 verified; workspace has no Git metadata.`). One cosmetic deviation: Step 3 fails as `ImportError: cannot import name 'baseline'` rather than the predicted `ModuleNotFoundError` wording, same subclass family as Task 3's flag.
- Task 4: review Approved, 0 Critical/Important, 2 Minors deferred to the whole-branch review. Reviewer falsified live: payload key spellings match what Tasks 6/7 consume; overwrite refusal fires before any filesystem mutation and a failing `os.replace` leaves no partial file; `load_baseline` covers missing-file, directory, non-JSON, non-dict, wrong-version branches; both baseline variants satisfy the single schema's `required` set; line-shift moves `location.line` while all three fingerprints stay byte-identical. Minors: (a) theoretical midnight-crossing flake in the byte-stability test (date re-derived per build; `SOURCE_DATE_EPOCH` pins it); (b) TOCTOU between the exists-guard and `os.replace`, moot for the single-writer CLI.
- Task 4: complete (no commits: no Git metadata; Approved verdict with falsification evidence; controller sanity check of the tree matched the implementer's 199 passed).
- Task 5: implementer's first dispatch blocked clean on a second plan-mandated test defect (`test_baseline_inspect_reports_both_variants` never flushed capsys between the text-mode `create` output and the `inspect --format json` parse; `json.loads` choked on the polluted stream). Ruling: accepted the implementer's one-line fix (a `capsys.readouterr()` flush), applied to plan and brief, task redone — DONE (`205 passed`, 0 warnings; `Checkpoint 5 verified; workspace has no Git metadata.`). Implementer also disclosed a self-corrected revert slip from the first attempt (an inverted Edit briefly duplicated the tests; caught immediately, restoration verified byte-exact at `199 passed` before the redo).
- Task 5: review Approved, 0 Critical/Important, 2 Minors deferred. Reviewer falsified live: exactly one `analyze_project` call for every create variant (including refused and `--allow-invalid` runs) and zero for inspect, across both module bindings; all exit-code contracts (1/2 with sentinel bytes untouched, --force recovery, missing file, non-JSON); relative `--output` resolves under root and absolute passes through; `queries=materialize_queries(...)` genuinely wired into the embedded report; exactly 3 files changed. Minors: (a) `_baseline_summary` direct-indexes keys `load_baseline` never checks, so a tampered-but-version-1 baseline crashes `inspect` with a raw KeyError instead of exit 2 — plan-mandated, hardening candidate; (b) `inspect` runs after `load_config`, so a broken config fails the command that needs no config — brief-mandated placement, fail-fast defensible.
- Task 5: complete (no commits: no Git metadata; Approved with falsification evidence; sanity run 43 CLI tests green).
- Task 6+7: implementer DONE after one blocked round (`220 passed`, 0 warnings; both checkpoints echoed no-Git; Task 6's designed Step-7 red state captured exactly: all structural tests failing on the missing `compare` attribute). First dispatch had stopped clean on the fixture defect above; redo was byte-verbatim.
- Task 6+7: fix round 1/5 (1 Important + 1 Minor addressed, 1 Minor addressed in report text; 0 open). The fix-round dispatch died on an account usage limit before touching the tree (verified byte-identical to task-6-after), so per the Task 1 precedent the controller executed the ruled fix directly. TDD held both directions: the first cut of the pinning test passed vacuously against the buggy code (100% coverage made the 100.0 gate pass), so the controller sharpened the fixture with an approved-but-unverified REQ-2 (coverage 50%) — RED reproduced the reviewer's exact phantom regression (`min-verification-trace`, threshold 100.0, actual 50.0), then the ruled `derived_suppressed = configuration_differs or date_differs` made it green (diff suite 16 passed; full suite `221 passed`, 0 warnings), and reverting only the predicate re-failed the test, with restoration verified byte-identical. Plan and brief 6 synced to the sharpened test. Independent re-review of the fix is deferred until the account limit resets; the fix1 package sits at reviews/task-6-fix1-package.md and the Task 8 review gate will re-cover diff.py.
- Task 6+7: complete (no commits: no Git metadata; Approved verdict plus controller-executed fix round with two-directional falsification; expected count back to sixteen, which the suite confirms).
- Task 8: implementer DONE after one blocked round (same capsys class, ruled above) — `227 passed`, 0 warnings; `Checkpoint 8 verified; workspace has no Git metadata.` All five brief code blocks byte-identical.
- Task 8: review Approved (0 Critical/Important, 2 Minors) and the same dispatch covered the deferred Task 6+7 fix-round re-review — also Approved: the fix1 contents verified in the tree, and a live mutation experiment (reverting only the suppression predicate re-fails exactly the new test, restoration byte-identical) proved the pinning test load-bearing. Task 8 falsified live: one-analysis counts across all variants (missing baseline = 0 analyses, diagnostic = 1 then exit 2); exit codes 0/1/2 as contracted; schema validates five payload shapes including representation-changes; strict-profile gate regression exits 1 while advisory exits 0; every text-render category exercised. Minors: (a) diagnostic baseline costs one analysis before its exit-2 refusal — conformant, the guard belongs to compare; (b) the `--recompute-with` argparse help still overpromised ("Re-evaluate both sides...") after the recompute amendment — RULING: corrected the string in plan and briefs 8/10 now; the live `diff` help string is corrected inside Task 10's implementation round (one string, no test asserts it) so snapshot ranges stay coherent.
- Task 8: complete (no commits: no Git metadata; Approved both jobs with falsification evidence).
- Task 9: implementer DONE after one blocked round (the DOC-1 ordering defect introduced by my own ruling, see above) — `234 passed`, 0 warnings; `Checkpoint 9 verified; workspace has no Git metadata.` Both files byte-identical to the corrected brief; the ImportError-vs-ModuleNotFoundError wording deviation flagged once more (cosmetic, recurring).
- Task 9: review Approved, 0 Critical/Important, 2 observational Minors (no action). Falsified live: deleting the baseline-side loop of `_union_edges` re-fails exactly the union test (restoration verified by sha256); direction semantics proven on all four authored directions including the negative ones; BFS diamond reports the shortest route; an 11-hop chain stops at distance 10 in 0.6 ms; guards refuse before any compare work; `_priority` both branches exercised (removed node reads the baseline's stored attributes); purity holds under patched-out filesystem. Minors: (a) `_priority`'s baseline fallback is unreachable through `analyze` (removed nodes become origins, and origins are excluded from impacted) — defensive depth, correct when called; (b) `_origins` dedup order shadows a relation-removed label under a same-node modification — no shipped scenario hits it, propagation unaffected.
- Task 10: implementer DONE on the first dispatch — `237 passed`, 0 warnings; no Git metadata. Includes the ruled one-string correction of the live `diff` `--recompute-with` help text. Implementer's two flags both assessed by the review: `_impact` returning 0 unconditionally is spec-conformant (ImpactReport carries no gates — impact explains propagation, it does not gate policy); the text-format impacted rendering was smoke-verified manually since the brief specifies exactly 3 tests.
- Task 10: review Approved, 0 Critical/Important of Task 10's own, but 1 finding ELEVATED by controller ruling to a real fix: `impact` does not reject a reference-date mismatch — the spec says "impact rejects the mismatch" for BOTH comparison axes, and the reviewer falsified live that a date-mismatched baseline traverses silently with exit 0 (root cause: the plan's Task 9 `analyze` guard compared only configurationFingerprint; impact.py was outside Task 10's file scope, hence the reviewer's Minor). Ruling: extend the `analyze` guard to nest configuration and referenceDate checks under `if not recompute:`, each with its own message, plus a pinning test (baseline dated 1999-01-01 raises without recompute, succeeds with) — applied to plan and brief 9, fix round dispatched to the Task 9 implementer. Other review falsifications: one-analysis counts across all variants (missing baseline = 0 analyses; dispatch-placement mutation yields 2); exit codes 0/1/2 with precedence hand-traced (structural failure beats diagnostic-baseline refusal); schema validated on propagating payloads with negative controls; both help strings corrected; exactly five cli.py hunks.
- Task 9 fix round (elevated from Task 10's review): implementer applied the amended brief byte-verbatim — TDD red was exactly `Failed: DID NOT RAISE ImpactError` (the falsified spec violation), green at 8 impact tests, full suite `238 passed`, 0 warnings. Re-reviewer verdict "All findings addressed, no new Critical/Important breakage": hand-traced the nested guards, re-ran the falsification independently (removing only the date check re-fails only the new test; restoration checksummed), proved the CLI surface live (date-mismatched baseline exits 2 with the date message; `--recompute-with current` exits 0), and confirmed dates-match runs unchanged.
- Task 11: the dispatch died on a second account usage limit AFTER landing the tests, Makefile targets, golden baseline, and all three docs; the controller verified every piece against the brief, applied two post-mortem rulings, and completed the acceptance run (per the Task 6+7 precedent). Rulings: (a) the showcase impact test and the `impact-example` target pass `--recompute-with current`, because the checked-in golden baseline necessarily predates any later run day and impact (per spec, enforced since the Task 9 fix round) rejects a reference-date mismatch — without this the showcase breaks every midnight; plan and brief 11 synced. (b) The determinism gate hashes `_extensions/quarto-needs/generated-index.lua`, the Lua index's real path.
- Task 11 acceptance: full suite `241 passed`, 0 warnings (before and after renders); two consecutive syncs byte-identical by SHA-256; `check-example` 85 objects 0 errors 0 warnings; `quality` exit 0; `query` exit 0; `baseline-example` + `diff-example` (`No changes.`) + `impact-example` (`No changes to propagate.`) all exit 0; HTML/DOCX/PDF rendered; rendered-book greps clean (re-run against the populated HTML site after the first pass was caught running vacuously against the PDF-only `_book`); preview server 404 during the PDF-only window then 200 after the planned final HTML re-render, never stopped or restarted; checkpoint `Milestone 3 verified; workspace has no Git metadata.`
- Task 11: review Approved, 0 Critical/Important, 2 plan-inherited Minors deferred (unused `_json` import + stale "reads the JSON" note in the impact showcase test; plan prose naming `generated-index.lua` without its full path). Falsified live: moving the golden baseline aside fails exactly the round-trip and schema tests, restoration checksummed; the golden payload validates and diffs empty with exit 0; date-stability confirmed under a shifted `SOURCE_DATE_EPOCH` (diff notices but stays empty/exit 0 — only impact needed the ruled flag, exactly as ruled); all eight README points checked one-by-one against the brief.
- Task 11: complete (no commits: no Git metadata; Approved with falsification evidence).

## Whole-branch review (2026-08-26, session 2)

- Verdict: **Milestone 3 closed.** No Critical; no milestone-introduced Important. Deferred-Minors adjudication: FIX NOW (4, applied by controller immediately after, all verified): (1) config.py docstring dropped the vacuous path-exclusion clause; (2) byte-stability test pins `SOURCE_DATE_EPOCH` (midnight flake); (3) `_baseline_inspect` wraps the summary in `except KeyError` -> exit 2 with a clean message (probed live: tampered v1 baseline now exits 2, no traceback); (4) unused `_json` import deleted from the impact showcase test. ACCEPT AS DOCUMENTED (5): write TOCTOU (atomic `os.replace`, single-writer CLI), inspect-after-load_config (README documents "broken configuration exits 2 from every command"), `_priority` fallback (defensive depth; instrumented 18/18 snapshot-path), `_origins` label shadowing (presentational; propagation intact via the union graph), missing-baseline exit 2 (test-pinned as usage error; exit 3 reserved for write-side).
- Carried into Milestone 4A planning (pre-existing, verified against task-1-before): `export --output` and `scan` on unwritable paths raise raw PermissionError tracebacks and exit 1 instead of the spec's exit 3 — wrap both in OSError guards; README's exit-code table lacks code 3; impact's config/date rejection and the recompute-suppresses-derived nuance deserve a README sentence.
- Cross-cutting sweep: single-analysis guarantee holds across every command (counters on all bindings, including 0-analysis paths); schemas all check_schema-clean and live outputs validate; golden baseline diffs clean and two syncs byte-identical; no unused imports or dead code beyond the fixed ones; docs match the amended semantics. Post-fix suites: affected groups 84 passed; full suite `241 passed in 259.87s`, 0 warnings. Snapshot: `snapshots/whole-branch-fixes`.

All eleven tasks complete: 175 (baseline) -> 241 tests, zero warnings, every task review-gated with live falsification, deferred Minors adjudicated by the whole-branch review above (4 fixed, 5 accepted as documented), milestone closed.
- Task 11: implementer DONE (`241 passed`, 0 warnings; `Milestone 3 verified; workspace has no Git metadata.`). Acceptance evidence reported: `make diff-example` -> `No changes.` exit 0 and `make impact-example` -> `No changes to propagate.` exit 0, both clean on the baseline's first creation, so no fingerprint was reading derived data; `make render-example-all` exit 0 for HTML/DOCX/PDF with no `need-ref-missing`, no unexpanded `{{<`, and no raw Mermaid source; `_book` re-rendered to HTML afterwards; preview on 8777 probed only, never restarted.
- Task 11: first review dispatch died early on an account session limit (reset 14:30), not on a code defect; re-dispatched at 15:01. This is the third limit interruption of the run (Task 1 review, Task 3 fix round, Task 11 review) — none corrupted the tree, because each dispatch either completes atomically or leaves the snapshot pair intact for re-diffing.
- Task 11: review returned Needs fixes — 3 Important, 2 Minor. Documentation checked out completely: all eight required README statements verified against the actual implementation, including both counter-intuitive rules and, critically, the correct asymmetry between `diff` (notices and suppresses) and `impact` (refuses) — the one pairing that would most mislead a CI author if documented backwards.
- Ruling: finding 1 is real and load-bearing, and the fix proceeds. `_diff` returns `profile_exit_code(profile, False, len(report.gate_regressions))`, so its exit code reflects ONLY gate regressions — never object, relation, relocation, finding, or metric changes. The reviewer reproduced this live in a scratch copy: changing `IAM-SYS-001`'s priority yields `"empty": false` with a populated `objects.modified`, and the CLI still exits 0. The milestone's headline round-trip guard therefore cannot fail on real content drift. Plan-mandated: the assertion is verbatim from my brief — if wrong, we tighten a test that was already adequate, which is the cheap direction.
- Ruling: finding 2 compounds finding 1 into a tautology and must be fixed in the same round. `diff.py` forces `gate_regressions = ()` whenever `derived_suppressed` (configuration OR reference date differs), and `_diff`'s exit code depends only on that count — so from the day after the baseline's stored `referenceDate` onward, the exit code is structurally pinned to 0 regardless of content. The checked-in baseline is dated 2026-08-26, i.e. today, so the gate stops meaning anything tomorrow. The reviewer verified by forcing `SOURCE_DATE_EPOCH` to the next day. Fix: pin `SOURCE_DATE_EPOCH` to the baseline's own stored date, read from the artifact so it cannot decay on regeneration.
- Ruling: finding 3 is accepted and is the implementer's own prose, not brief-mandated. `CONTRIBUTING.md` claims the pytest test "enforces" that the baseline "must always diff clean"; it enforces the materially weaker "no gate regression", and the discriminating check it names (`make diff-example` printing `No changes.`) is manual. Documentation that overstates a guarantee is worse than none, because a contributor will trust it.
- Ruling: the Minor about the stale roadmap (README still lists baseline/diff/impact under "0.5 — Change management" as future work after they shipped) is folded into the same round rather than deferred, since the same file is already open and leaving it contradicts the section added directly above it.
- COORDINATION NOTICE (controller session quarto-needs-60, 2026-08-26 15:30): two sessions executed Task 11 against this same ledger in parallel. Session quarto-needs-9c had already completed Task 11, run the full acceptance gate, and moved on to Milestone 4A (plan + workspace + strict xfail markers for the CSV/SARIF/JUnit writers). This session, unaware of that, dispatched its own Task 11 implementer and reviewer. No damage: full suite after both sets of edits is `245 passed, 2 xfailed in 126.83s`, the 2 xfailed being 9c's deliberate strict markers.
- Ruling: this session's duplicate Task 11 fix round is KEPT rather than reverted, because its review found a defect 9c's review approved. The round-trip test asserted only `cli.main([...]) == 0`, and `_diff`'s exit code reflects only `len(report.gate_regressions)` — reproduced live, a modified object attribute yields `"empty": false` with exit 0. Worse, `derived_suppressed` empties `gate_regressions` whenever the reference date differs, so the assertion becomes tautological from the day after the baseline's stored date. The fix asserts `payload["empty"] is True` and `payload["notices"] == []` from `diff --format json`, pinning `SOURCE_DATE_EPOCH` to the date read out of the baseline artifact itself so a regeneration cannot silently re-break it; falsified both directions (mutated attribute fails the new assertion while passing the old one; shifted date fails the new one too) — if wrong, the test is stricter than the milestone gate strictly required, which is the safe direction.
- Ruling: the parallel-session collision itself is surfaced to the user rather than resolved unilaterally. Deciding which session owns the repository is not a controller call, and continuing to execute would keep duplicating work and consuming the shared account quota — the three session-limit interruptions this run hit were plausibly the two sessions competing for it.

## Final whole-branch review

- Verdict: Ship after fixes — 0 Critical, 4 Important, 9 Minor, backed by 9 runtime probes. No merge artifacts or duplicated definitions found despite the parallel-session collision. Fingerprint contract verified end to end: traced an object and a relation through records -> fingerprints -> baseline serialization -> diff comparison with no line number, `href`, or generated metric leaking at any hop.
- Ruling: all 4 Important findings are fixed in one wave. (1) `impact --recompute-with current` leaves half the traversal under the old policy — `_union_edges` reads each baseline relation's *stored* `impactDirection` while `_recomputed_relations` drops that key entirely, so a catalog whose impact direction changed silently deletes baseline-only edges; probed with an edge present only in the baseline yielding `[]` instead of the honest result. This violates the spec's "ensuring one relation policy governs the entire traversal" and contradicts README. (2) Three new commands exit 1 with raw tracebacks where the spec fixes 2 or 3. (3) `test_aegis_impact_explains_a_removed_verification` asserts only the exit code — the same passes-for-the-wrong-reason pattern this milestone hit four times, and it sits twelve lines below a docstring explaining why exit code alone is not a gate. (4) `empty: true` conflates "nothing changed" with "we did not look", so a strict-profile CI running `diff --recompute-with current` across a day boundary gets exit 0 / empty / no notices from a comparison that evaluated no gate.
- Ruling: five Minors are folded into the same wave because each is cheap and each is a false or decaying claim rather than a polish preference — the false comment in `test_fingerprints.py` (ablation-probed: deleting `priority`/`tags` from the digest leaves both tests passing), the malformed README exit-code section, the untested strict-profile exit 1, the `make diff-example` invariant that only holds on the baseline's own day, and the untightened diff schema item shapes.
- Ruling: four Minors are PARKED rather than fixed. `OBJECT_FIELDS` reporting `["attributes"]` instead of naming priority/tags is a serialization design change the spec does not require; `MAX_DISTANCE` truncating without a `truncated` flag touches the spec's deferred "impact traversal policies"; relation-change origins naming only the authored source, and `seen.setdefault` collapsing a doubly-classified object, are both documented as defensible and reachable by no shipped scenario. Each is real and none is load-bearing — if wrong, they surface as reporting coarseness, never as a wrong verdict.
- Ruling: the deferred minors from Tasks 1, 2, and 11 and the carried pre-existing exit-3 gap are all triaged as resolved or carry-only by the final review; the exit-3 gap was in fact already fixed inside this range and is superseded by the new gaps in Important 2.
- Note: the working tree is ahead of the reviewed range — `src/quarto_needs/exporters/csv_export.py` and `tests/test_export_csv.py` are Milestone 4A work from the parallel session, landed after the `task-11-fix1` snapshot. The fix wave must not touch them.
- Final fix wave: the implementer died on an account limit before writing its report, so the re-review had no self-attestation to check against and rested entirely on code inspection, schema validation, and its own ablation runs.
- Final fix wave re-review verdict: all 4 Important and all 5 folded Minors ADDRESSED; no new Critical/Important breakage in scope; **Ship: Yes**. Important 1 was verified by ablation rather than by reading: reverting both the `recomputed_relations` field and the `impact.analyze` wiring in a scratch copy made `test_impact_rejects_a_configuration_mismatch_without_recompute` fail with `StopIteration` (TC-1 never reached), and the actual fix passes — proving the recomputed list genuinely reaches `_union_edges` instead of the field merely existing.
- Ruling: residual gap (a) — the new `KeyError`→`DiffError`/`ImpactError` translation and the three `OSError`→exit-3 CLI guards have no dedicated unit test; the re-reviewer verified all of them by direct and monkeypatched invocation instead. PARKED. They are defensive translations of already-tested exit codes, and the process allows no second fix wave — if wrong, a future refactor could silently drop a guard and only a traceback in the field would reveal it. Worth a test in Milestone 4A, where CLI failure semantics are already in scope.
- Ruling: residual gap (b) — `schemas/diff-v1.schema.json`'s tightened `relations.added` and `relations.representationChanged` item shapes are still never exercised by a populated payload, `representationChanged` having its own required `from`/`to` shape. PARKED. The schema is strictly better than the `{"type": "object"}` it replaced and the other five categories are now validated populated — if wrong, a malformed representation-change entry ships unvalidated, which degrades a diagnostic rather than a verdict.
- Ruling: the skill directs deleting this workspace once the final review is clean, on the grounds that "the git history is the record now". That does not hold here: this repository has **no Git metadata**, so the workspace is the *only* record of the milestone's execution — ledger, briefs, reports, and review packages included. The workspace is KEPT — if wrong, it costs disk space; deleting it would have destroyed the entire audit trail of a milestone executed without version control.
- Note: the parallel session has advanced well into Milestone 4A (CSV/SARIF/JUnit/Markdown exporters, vendored SARIF schema) and drafted a Milestone 5A plan. Its `exporters/markdown_export.py` already consumes the `suppressed` field this fix wave added, so the two lines of work are consistent rather than conflicting.
