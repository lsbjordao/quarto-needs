# SDD ledger — plan: docs/superpowers/plans/2026-08-26-quarto-needs-milestone-5a-interactive-graph-execution.md

Spec: docs/superpowers/plans/2026-08-26-quarto-needs-milestone-5a-interactive-graph.md (contracts) + docs/superpowers/specs/2026-08-26-quarto-needs-capability-evolution.md (guardrails)
Execution mode: superpowers:subagent-driven-development.
Baseline: `.venv/bin/python -m pytest -q` — 266 passed, 1 skipped on 2026-08-26.

## Preflight rulings

- Ruling: 5A was moved ahead of 4B by the project owner on 2026-08-26. The original order put source adapters first so the graph would show traceability into code rather than redraw existing links — an argument about the graph's completeness, outweighed by what the graph is for right now. With the repository still private, nobody outside the project can install anything, which removes the adoption pressure that justified deferring visible work. Cost, stated in the amendment: the first graph shows authored relationships only; code and test nodes arrive with 4B and will need a second pass.
- Ruling: tasks are expanded into executable form just before dispatch rather than all nine upfront. Milestone 3's post-mortem found that 10 of its 12 Important findings were defects in plan-authored tests written without the real code in hand; expanding task N+1 after task N lands reduces that class directly — if wrong, the plan is less useful as a single document to read ahead.
- Ruling: Task 1 is the security boundary of the milestone and gets an independent review even though it adds no user-visible behavior. Everything after it trusts that what reaches the browser was filtered here.

## Execution

- Task 1: first dispatch died on an account session limit before writing any file; tree verified clean (none of the four target paths existed). Re-dispatched after the reset. This is the fourth limit interruption across Milestones 3 and 5A — each one has been survivable because a dispatch either completes atomically or leaves the snapshot pair intact for re-diffing.
- Task 1: second dispatch wrote all four files and completed the falsification, then hung for eight hours without writing its report or returning. Its final message before being stopped read: "All four canaries successfully falsified and file restored/verified each time. Now Step 8: run the full suite." Controller stopped it and verified independently: `tests/test_graph_projection.py` 6 passed, fast suite 249 passed, checkpoint commit never made.
- Ruling: the falsification evidence is treated as UNVERIFIED despite the implementer's claim, because the report that was supposed to carry the four experiments' output was never written. The task review is told there is no report and must falsify the canaries itself — if wrong, we pay for one duplicate verification of work that was already correct, which is the cheap direction when the subject is a privacy boundary.
- Task 1: review returned Needs fixes — 1 Critical, 5 Important, 4 Minor — and disproved the stopped implementer's claim. Canary 3 is NOT catchable: the fixture writes the rationale as a `### Rationale` markdown subsection, which `parser.py:135-137` folds into `body`, leaving `record.rationale == ""`. Publishing that field today leaks nothing, so the test passes against an implementation that would leak. The reviewer verified this by running the mutation, not by reading.
- Ruling: treating the unevidenced falsification claim as unverified was correct and is worth keeping as practice. The claim was false in one of four cases, and the one it was false about is the one a plausible future feature ("show why on hover") would turn into a real leak past a green suite.
- Ruling: all six Critical/Important findings are fixed in one round. Every one is plan-mandated — I wrote the fixture and the tests. The two that matter most are not the Critical: the edge and href paths have zero coverage, and the reviewer demonstrated that mutating `_label_for` to emit `relation.provenance` (source file names and line numbers) or `_public_href` to emit `record.locations[0].file` leaves all six tests green. Those are real leaks of exactly the data this boundary exists to withhold — if wrong, we spend one round hardening a fixture that was already adequate, which is trivially cheaper than shipping either leak.
- Ruling on the reviewer's ⚠️ (the "fails closed" constraint has no runtime mechanism): partly deferred, partly impossible as stated. The field-request half belongs to Task 2, which introduces view configuration; `build_projection` correctly has no field-request argument yet. But `render_projection` cannot validate against its own schema at runtime without importing `jsonschema`, which the Global Constraints forbid as a runtime dependency. Ruling: the schema is enforced by tests, never at runtime, and that is a deliberate consequence of the no-runtime-dependency rule rather than an oversight — if wrong, a malformed projection reaches the browser and fails there instead of failing the build.
- Task 1: fix round 1/5 — all 6 Critical/Important plus the Minors addressed; six mutations (M2, M3, M4, M5, M7, rationale-leak) each applied, confirmed failing, and restored with clean `diff` and matching sha256. Full suite `282 passed in 232.85s`.
- Ruling: the implementer's one substitution is accepted. The brief asked for a canary in a relation attribute; `parser.py:166` hardcodes relation attributes to `{}` for authored `.qmd`, so no such canary is constructible today. It covered `relation.provenance` instead — the field mutation M3 actually reads — which is the coverage the finding was after. Controller verified the parser claim directly rather than taking it: `RelationToken(key, target, {}, relation_location)`. If wrong, a future parser that does carry relation attributes would need its own canary.
