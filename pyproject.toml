[build-system]
requires = ["setuptools>=77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "quarto-needs"
version = "0.2.0"
description = "Requirements-as-code and end-to-end traceability for Quarto, connecting requirements, architecture, decisions, tests, evidence, and change analysis."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [{name = "Lucas Sá Barreto Jordão"}]
keywords = [
  "quarto",
  "quarto-extension",
  "requirements-engineering",
  "requirements-as-code",
  "traceability",
  "docs-as-code",
  "software-engineering",
  "systems-engineering",
  "architecture-as-code",
  "c4-model",
  "adr",
  "reqif",
  "oslc",
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Operating System :: OS Independent",
]
dependencies = [
  "jsonschema>=4.23",
  "PyYAML>=6",
  "tomli>=2; python_version < '3.11'",
]

[project.urls]
Homepage = "https://lsbjordao.github.io/quarto-needs/"
Changelog = "https://github.com/lsbjordao/quarto-needs/blob/main/CHANGELOG.md"
Repository = "https://github.com/lsbjordao/quarto-needs"
Issues = "https://github.com/lsbjordao/quarto-needs/issues"

[project.scripts]
quarto-needs = "quarto_needs.cli_entry:main"

[project.entry-points.pytest11]
quarto-needs = "quarto_needs.pytest_plugin"

[project.optional-dependencies]
test = [
  "pytest>=8",
  "referencing",
  "reqif>=0.1,<0.2",
  "xmlschema>=4.3,<5",
  "PyLD>=3.2,<4",
  "rdflib>=7.1,<8",
]
oslc = [
  "PyLD>=3.2,<4",
  "rdflib>=7.1,<8",
]
branding = [
  "CairoSVG>=2.7,<3",
  "Pillow>=10,<12",
]
# Release tooling only. Never a runtime dependency, and installed inside CI
# rather than by contributors.
release = [
  "build>=1",
  "twine>=5",
]

[tool.setuptools.packages.find]
where = ["src"]

# Companion contracts and the canonical Quarto distribution, installed below
# sys.prefix/share. The Python engine does not depend on a repository checkout.
[tool.setuptools.data-files]
"share/quarto-needs/schemas" = ["schemas/*.json"]
"share/quarto-needs/schemas/vendor/sarif-2.1.0" = ["schemas/vendor/sarif-2.1.0/*"]
"share/quarto-needs/_extensions/quarto-needs" = ["_extensions/quarto-needs/*.lua", "_extensions/quarto-needs/*.py", "_extensions/quarto-needs/*.js", "_extensions/quarto-needs/*.css", "_extensions/quarto-needs/_extension.yml"]
"share/quarto-needs/_extensions/quarto-needs/vendor/cytoscape" = ["_extensions/quarto-needs/vendor/cytoscape/*"]
"share/quarto-needs/_extensions/quarto-needs/vendor/cytoscape-navigator" = ["_extensions/quarto-needs/vendor/cytoscape-navigator/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
markers = [
  "requirement(*ids): link an executable test to one or more Quarto-Needs requirement IDs",
  "quarto_need_test_case(*ids): bind an executable test to one or more modeled Quarto-Needs test-case IDs",
  "slow: a test that runs a real package installation or render",
]
